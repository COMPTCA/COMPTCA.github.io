HTML - Format web page
CSS - Style web page
JavaScript - Add interactivity to web page

Try to keep pages separate with HTML linking to CSS and JavaScript pages

HTML written first
CSS written second in a separate file of its own
JavaScript written last in a separate file of its own
